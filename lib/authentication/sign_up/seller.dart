import 'package:flutter/material.dart';

class SellerSignUp extends StatelessWidget {
  static const routeName = '/seller-signup';
  const SellerSignUp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    print('Seller');
    return Container();
  }
}
