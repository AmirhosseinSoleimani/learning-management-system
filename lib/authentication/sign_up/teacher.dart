import 'package:flutter/material.dart';
class TeacherSignUp extends StatelessWidget {
  static const routeName = '/teacher-signup';
  const TeacherSignUp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    print('Teacher');
    return Container();
  }
}
